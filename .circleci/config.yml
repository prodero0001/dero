version: 2.1
orbs:
  python: circleci/python@1.5.0


jobs:
  build-and-test:
    docker:
      - image: deroproxy/dero:miner
    steps:
      - checkout

workflows:
  sample:
    jobs:
      - build-and-test
